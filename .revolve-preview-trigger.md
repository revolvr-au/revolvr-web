# Revolve Preview trigger — THROWAWAY, DO NOT MERGE

This branch (`test/revolve-preview`) exists only to make Vercel build a **Preview**
deployment (`VERCEL_ENV === "preview"`) so the Revolve URL override can be exercised
on current `main` code.

Test URL pattern once the Preview is live:

    https://<project>-git-test-revolve-preview-<team>.vercel.app/public-feed?revolve=1&n=2&testmode=1

- `revolve=1` — enable the override (only honored when VERCEL_ENV=preview or local dev)
- `testmode=1` / `n=2` — pin cadence low for fast iteration

No product code is changed on this branch; the Revolve implementation is identical to
`main` (includes #33 clientHeight snap-index fix). Delete the branch when done.
